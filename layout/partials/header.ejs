<header id="header">
  <div id="nav">
    <ul>
      <% for (var i in theme.menu) { %> 
        <li><a href="<%- url_for(theme.menu[i]) %>"><%= __('menu.'+i).replace('menu.', '') %></a></li>
      <% } %>
    </ul>
  </div>
</header>